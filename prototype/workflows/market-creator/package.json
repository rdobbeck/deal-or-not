{
  "name": "market-creator-workflow",
  "version": "1.0.0",
  "description": "CRE workflow to auto-create prediction markets when agent games are created",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@chainlink/cre-sdk": "^1.1.1",
    "viem": "^2.34.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
